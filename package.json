{
    "name": "evide-digital-signage",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix client\" \"json-server --watch db.json --port 5000\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "json-server": "^0.17.3"
    }
}
